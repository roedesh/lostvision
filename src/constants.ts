export const NATIVE_WIDTH = 1024;
export const NATIVE_HEIGHT = 768;

export const TILE_SIZE = 16;
export const MAP = {
  w: NATIVE_WIDTH / TILE_SIZE,
  h: NATIVE_HEIGHT / TILE_SIZE,
};

export const SPEED = 3;
export const JUMP_HEIGHT = 12;
export const MAX_GRAVITY = 50;

export const FPS = 60;
export const STEP = 1 / FPS;
